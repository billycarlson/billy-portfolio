---
import TopNav from "../components/TopNav.astro";
import Footer from "../components/Footer.astro";
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="alternate icon" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="robots" content="noindex, nofollow" />
		<meta name="generator" content={Astro.generator} />
		<title>Billy Carlson - Product Design Director</title>
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:wght@400;500;600;700&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
		<link rel="stylesheet" href="/styles/case-study.css" />
	</head>
	<body>
		<TopNav />
		
		<main class="home-page">
			<!-- Hero Section -->
			<section class="home-hero">
				<h1 class="home-headline">Hi, I'm Billy</h1>
				<p class="home-subheadline">
					I'm a UX Design Manager at iHerb. My mission is to bridge the gap between user needs and business goals—creating products that people genuinely like to use and fuel sustainable growth.
				</p>
				<p class="home-description">
					Say hi!
				</p>
			</section>

			<!-- About Section -->
			<section class="home-section">
				<div class="section-header">
					<span class="section-label">About Me</span>
					<h2 class="section-title">Design Leadership & Strategy</h2>
				</div>
				<div class="about-content-grid">
					<div class="about-image-wrapper">
						<div class="placeholder-image product-mockup"></div>
					</div>
					<div class="home-content">
						<p class="home-text">
							I bring a focus to communication, systems thinking, and creative strategy across end-to-end product design. I also coach designers through structured feedback and practical skill development.
						</p>
						<a href="/about" class="home-link">Learn More</a>
					</div>
				</div>
			</section>

			<!-- Case Studies Section -->
			<section class="home-section">
				<div class="section-header">
					<span class="section-label">Work</span>
					<h2 class="section-title">Case Studies</h2>
				</div>
				<div class="work-grid">
					<a href="/case-studies/iherb-desktop-redesign" class="work-item">
						<div class="work-image-wrapper">
							<div class="placeholder-image product-mockup"></div>
						</div>
						<span class="work-label">Case Study</span>
						<h3 class="work-title">iHerb Desktop Homepage Redesign</h3>
						<p class="work-description">Reframing a high-pressure, executive-driven request into a future-facing design vision</p>
					</a>
					<a href="/case-studies/momentum-report" class="work-item">
						<div class="work-image-wrapper">
							<div class="placeholder-image product-mockup"></div>
						</div>
						<span class="work-label">Case Study</span>
						<h3 class="work-title">Momentum Report</h3>
						<p class="work-description">Transforming a manual reporting practice into a digital-first insights product</p>
					</a>
					<a href="/work" class="work-item">
						<div class="work-image-wrapper">
							<div class="placeholder-image geometric-pattern"></div>
						</div>
						<span class="work-label">View All</span>
						<h3 class="work-title">See All Case Studies</h3>
						<p class="work-description">Explore more of my work and projects</p>
					</a>
				</div>
			</section>

			<!-- Links Section -->
			<section class="home-section">
				<div class="section-header">
					<span class="section-label">Connect</span>
					<h2 class="section-title">Get In Touch</h2>
				</div>
				<div class="links-grid">
					<a href="https://linkedin.com/in/billycarlson" target="_blank" class="link-item">
						<span class="link-label">LinkedIn</span>
					</a>
					<div class="link-item">
						<span class="link-label">Speaking & Community</span>
					</div>
				</div>
			</section>

			<!-- Testimonials Section -->
			<section class="home-section">
				<div class="section-header">
					<span class="section-label">Testimonials</span>
					<h2 class="section-title">Kind Words from Collaborators</h2>
				</div>
				<div class="home-content">
					<p class="home-text" style="font-style: italic;">
						"Structured, transparent, and organized" • "High accountability and drive" • "Customer-centric approach" • "Practical and grounded in feasibility"
					</p>
				</div>
			</section>
		</main>

		<Footer />
	</body>
</html>

<style>
  .home-page {
    max-width: 1100px;
    margin: 0 auto;
    padding: 60px 80px 80px;
    width: 100%;
  }

  /* Hero */
  .home-hero {
    margin-bottom: 80px;
    max-width: 800px;
  }

  .about-content-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: start;
  }

  .about-image-wrapper,
  .work-image-wrapper {
    width: 100%;
    aspect-ratio: 4 / 3;
    overflow: hidden;
  }

  .work-image-wrapper {
    margin-bottom: 24px;
  }

  /* Quote section with red background */
  .quote-section-red {
    background: var(--red);
    padding: 60px;
    margin-left: -80px;
    margin-right: -80px;
    border: none;
  }

  .quote-section-red .home-quote {
    color: var(--white);
    font-style: normal;
    font-weight: 600;
  }

  .home-headline {
    font-size: 48px;
    font-weight: 500;
    color: var(--text);
    margin: 0 0 30px;
    font-family: 'Instrument Serif', serif;
  }

  .home-subheadline {
    font-size: 36px;
    line-height: 1.3;
    color: var(--text);
    font-weight: 400;
    font-family: 'Instrument Serif', serif;
    margin: 0 0 24px;
  }

  .home-description {
    font-size: var(--step-0);
    line-height: 1.7;
    color: var(--muted);
    margin: 0;
    max-width: 650px;
  }

  /* Sections */
  .home-section {
    margin-bottom: 80px;
    padding-top: 40px;
    border-top: 1px solid var(--hairline);
  }

  .section-header {
    margin-bottom: 40px;
  }

  .section-label {
    display: block;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: 0.78rem;
    color: var(--red);
    margin-bottom: 8px;
    font-weight: 600;
    font-family: 'Inter', sans-serif;
  }

  .section-title {
    font-size: 36px;
    font-weight: 500;
    color: var(--text);
    margin: 0;
    font-family: 'Instrument Serif', serif;
  }

  .home-content {
    max-width: 800px;
  }

  .home-text {
    font-size: var(--step-0);
    line-height: 1.7;
    color: var(--muted);
    margin: 0 0 24px;
  }

  .home-quote {
    font-size: var(--step-1);
    line-height: 1.6;
    color: var(--text);
    font-style: italic;
    margin: 0;
    max-width: 800px;
  }

  .home-link {
    font-size: var(--step-0);
    color: var(--text);
    text-decoration: none;
    font-family: 'Inter', sans-serif;
    transition: color 0.2s, text-decoration 0.2s;
    display: inline-block;
    margin-top: 8px;
  }

  .home-link:hover {
    color: var(--red);
    text-decoration: underline;
  }

  /* Placeholder image styles */
  .geometric-pattern {
    background: 
      repeating-linear-gradient(
        0deg,
        transparent,
        transparent 20px,
        var(--grey-medium) 20px,
        var(--grey-medium) 21px
      ),
      repeating-linear-gradient(
        90deg,
        transparent,
        transparent 20px,
        var(--grey-medium) 20px,
        var(--grey-medium) 21px
      ),
      var(--grey-dark);
    background-size: 100% 100%, 100% 100%, auto;
  }

  .product-mockup {
    background: var(--grey-dark);
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .product-mockup::before {
    content: "";
    position: absolute;
    width: 60%;
    height: 80%;
    background: var(--grey-medium);
    border-radius: 8px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
  }

  .product-mockup::after {
    content: "";
    position: absolute;
    width: 40%;
    height: 50%;
    background: linear-gradient(135deg, var(--grey-medium) 0%, transparent 100%);
    border-radius: 4px;
    transform: rotate(-10deg);
  }

  /* Work Grid */
  .work-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 40px;
  }

  .work-item {
    text-decoration: none;
    color: inherit;
    transition: opacity 0.2s;
    display: flex;
    flex-direction: column;
  }

  .work-title {
    transition: text-decoration 0.2s;
  }

  .work-item:hover {
    opacity: 0.7;
  }

  .work-item:hover .work-title {
    text-decoration: underline;
  }

  .work-label {
    display: block;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: 0.7rem;
    color: var(--red);
    margin-bottom: 8px;
    font-weight: 600;
    font-family: 'Inter', sans-serif;
  }

  .work-title {
    font-size: var(--step-1);
    font-weight: 500;
    color: var(--text);
    margin: 0 0 12px;
    font-family: 'Instrument Serif', serif;
  }

  .work-description {
    font-size: var(--step-0);
    line-height: 1.6;
    color: var(--muted);
    margin: 0;
  }

  /* Links Grid */
  .links-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 40px;
    max-width: 600px;
  }

  .link-item {
    text-decoration: none;
    color: inherit;
    transition: opacity 0.2s;
  }

  .link-item:hover {
    opacity: 0.7;
  }

  .link-item:hover .link-label {
    text-decoration: underline;
  }

  .link-label {
    font-size: var(--step-0);
    line-height: 1.6;
    color: var(--text);
    transition: text-decoration 0.2s;
  }

  /* Responsive */
  @media (max-width: 767px) {
    .home-page {
      padding: 40px 20px 60px;
    }

    .home-hero {
      margin-bottom: 60px;
    }

    .home-subheadline {
      font-size: 1.4rem;
    }

    .home-section {
      margin-bottom: 60px;
      padding-top: 30px;
    }

    .quote-section-red {
      padding: 40px 24px;
      margin-left: -20px;
      margin-right: -20px;
    }

    .about-content-grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .work-grid {
      grid-template-columns: 1fr;
      gap: 30px;
    }

    .links-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }
  }

  @media (min-width: 768px) and (max-width: 1023px) {
    .home-page {
      padding: 50px 40px 70px;
    }

    .quote-section-red {
      margin-left: -40px;
      margin-right: -40px;
    }

    .about-content-grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .work-grid {
      grid-template-columns: 1fr;
      gap: 30px;
    }
  }
</style>
