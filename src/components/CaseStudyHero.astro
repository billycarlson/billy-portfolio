---
const { title, lede, meta = [] } = Astro.props;
---

<header class="hero">
  <h1>{title}</h1>
  {lede && <p class="lede">{lede}</p>}
  {meta.length > 0 && (
    <div class="meta">
      {meta.map((m) => <span>{m}</span>)}
    </div>
  )}
</header>

